class Solution {
    public int countCommas(int n) {
        int use = 999;
        if(n>=1 && n<=998){
            return 0;
        }

        // int newN = n-1;
        // int ans = n - use;
        return n-use;
    }
}